# Handle optional dependencies properly
optional=false
legacy-peer-deps=true

# Use npm instead of yarn
package-lock=true

# Disable optional native dependencies that cause issues on Vercel
rollup-optional-dependencies=false 